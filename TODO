Done:
    git-ignore - to add files to the git ignore
    git-view - clear the screen and show the status

In progress:
    git-rmsubmodule - Remove a submodule from a project
    git-sync - add specified files, commit with a the specified message. push to the remote

TODO:

Stale:
    git-create - creates a github repo from the commandline
    git-toolsconfig - adds the username and token to the git-create script

